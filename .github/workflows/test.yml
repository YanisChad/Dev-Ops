name : test
on : push

jobs:
  test: test1
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run tests
        run: npm install
        run: npm test